server:
  port: 80

spring:
  application:
    name: cloud-order-servlet

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      #设置与Eureka Server 交互的地址。查询服务和注册服务都需要这个依赖这个地址。
      defaultZone: http://localhost:7001/eureka